// const jest = require('jest');
// const config = require('../../configs/jest');

// const argv = process.argv.slice(3);

// argv.push('--config', JSON.stringify(config));

// module.exports = () => {
//   process.env.BABEL_ENV = 'test';
//   process.env.NODE_ENV = 'test';

//   jest.run(argv);
// };
