---
menu: components
name: SlideModal
---

import { Playground, Props } from "docz";

import SlideModal from './SlideModal';
import SlideModalExample from './SlideModal.docz';

# SlideModal

<Props of={SlideModal} />

**Для того, чтобы модалка была не на весь экран, а была в контейнере надо в containerRef прокинуть ref контейнера, контейнеру сделать position: relative и overflow: hidden.**

## Basic code

```
const SlideModalExample = () => {
  const [open, setOpen] = useState(false);
  const containerRef = useRef();

  const handleOpen = () => {
    setOpen(true);
  };

  const handleClose = () => {
    setOpen(false);
  };

  return (
    <div ref={containerRef} style={{ position: 'relative', overflow: 'hidden' }}>
      <button type="button" onClick={handleOpen}>Open modal</button>
      <ThemeProvider theme={new AstralEDOTheme()}>
        <MainModal open={open} containerRef={containerRef} onClose={handleClose}>
          <MainModal.Title>
              Title
          </MainModal.Title>
          <MainModal.Content>
              Content
          </MainModal.Content>
          <MainModal.Footer>
              Footer
          </MainModal.Footer>
        </MainModal>
      </ThemeProvider>
    </div>
  );
};
```

## Medium modal

<Playground>
  <SlideModalExample />
</Playground>

## Small modal

<Playground>
  <SlideModalExample size="small" />
</Playground>

## Large modal в контейнере

<Playground>
  <SlideModalExample contain size="large" />
</Playground>
