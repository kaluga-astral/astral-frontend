---
menu: dashboard-layout
name: OrganizationSelector
---

import { Playground, Props } from "docz";
import { MemoryRouter } from "react-router-dom";
import { SaveIcon } from "@astral-frontend/icons";

import OrganizationSelector from "./OrganizationSelector";

# OrganizationSelector

<Props of={OrganizationSelector} />

## Basic code

```
  <OrganizationSelector disablePadding={true}>
    some MainPaper content
  </OrganizationSelector>
```

## Basic usage

<Playground>
  <MemoryRouter initialEntries={['/']}>
      <OrganizationSelector
        addLinkHref="/abonents/add"
        currentOrganization={{
          name: 'my Organization'
        }}
        NotFoundPlaceholder={() => (
          <OrganizationSelector.NotFoundPlaceholder addLinkHref="/abonents/add" />
        )}
      >
        <OrganizationSelector.Item name="second organization" onClick={() => console.log('click')}/>
        <OrganizationSelector.Item name="second organization" onClick={() => console.log('click')}/>
      </OrganizationSelector>
  </MemoryRouter>
</Playground>
