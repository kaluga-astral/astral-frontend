---
menu: dashboard-layout
name: Sidebar
---

import { Playground, Props } from "docz";
import { MemoryRouter } from "react-router-dom";
import { SaveIcon } from "@astral-frontend/icons";

import Sidebar from "./Sidebar";
import Product from "../Product";
import ActionButton from "../ActionButton";
import SidebarNav from "../SidebarNav";
import SidebarNavDropdown from "../SidebarNavDropdown";
import CurrentUserInfo from "../CurrentUserInfo";

# Sidebar

<Props of={Sidebar} />

## Basic code

```
      <Sidebar>
        <Product
          title="example-product"
          Logo={SaveIcon}
        />
        <ActionButton>
          <ActionButton.Icon>
            <SaveIcon />
          </ActionButton.Icon>
          <ActionButton.Text>Новое заявление</ActionButton.Text>
        </ActionButton>
        <SidebarNav>
          <SidebarNavDropdown
            text="Документы"
            Icon={SaveIcon}
          >
            <SidebarNavDropdown.Item text="Входящие" to="/documents/incoming" />
          </SidebarNavDropdown>
        </SidebarNav>
        <CurrentUserInfo avatarAlt="AA" avatarSrc={null} userName="userName">
          <CurrentUserInfo.Item Icon={SaveIcon} text="Выйти" />
        </CurrentUserInfo>
      </Sidebar>
```

## Basic usage

<Playground>
  <MemoryRouter initialEntries={['/']}>
      <Sidebar>
        <Product
          title="example-product"
          Logo={SaveIcon}
        />
        <ActionButton>
          <ActionButton.Icon>
            <SaveIcon />
          </ActionButton.Icon>
          <ActionButton.Text>Новое заявление</ActionButton.Text>
        </ActionButton>
        <SidebarNav>
          <SidebarNavDropdown
            text="Документы"
            Icon={SaveIcon}
          >
            <SidebarNavDropdown.Item text="Входящие" to="/documents/incoming" />
            <SidebarNavDropdown.Item text="Исходящие" to="/documents/outgoing" />
            <SidebarNavDropdown.Item text="Черновики" to="/documents/drafts" />
          </SidebarNavDropdown>
        </SidebarNav>
        <CurrentUserInfo avatarAlt="AA" avatarSrc={null} userName="userName">
          <CurrentUserInfo.Item Icon={SaveIcon} text="Выйти" />
        </CurrentUserInfo>
      </Sidebar>
  </MemoryRouter>
</Playground>
