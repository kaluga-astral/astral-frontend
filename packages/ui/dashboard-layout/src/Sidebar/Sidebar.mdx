---
menu: dashboard-layout
name: Sidebar
---

import { Playground, Props } from "docz";
import { MemoryRouter } from "react-router-dom";
import { SaveIcon } from "@astral-frontend/icons";

import Sidebar from "./Sidebar";
import Product from "../Product";
import SidebarActionButton from "../SidebarActionButton";
import AsideNav from "../AsideNav";
import AsideNavDropdown from "../AsideNavDropdown";
import CurrentUserInfo from "../CurrentUserInfo";

# Sidebar

<Props of={Sidebar} />

## Basic code

```
      <Sidebar>
        <Product
          title="example-product"
          Logo={SaveIcon}
        />
        <SidebarActionButton>
          <SidebarActionButton.Icon>
            <SaveIcon />
          </SidebarActionButton.Icon>
          <SidebarActionButton.Text>Новое заявление</SidebarActionButton.Text>
        </SidebarActionButton>
        <AsideNav>
          <AsideNavDropdown
            text="Документы"
            Icon={SaveIcon}
          >
            <AsideNavDropdown.Item text="Входящие" to="/documents/incoming" />
          </AsideNavDropdown>
        </AsideNav>
        <CurrentUserInfo avatarAlt="AA" avatarSrc={null} userName="userName">
          <CurrentUserInfo.Item Icon={SaveIcon} text="Выйти" />
        </CurrentUserInfo>
      </Sidebar>
```

## Basic usage

<Playground>
  <MemoryRouter initialEntries={['/']}>
      <Sidebar>
        <Product
          title="example-product"
          Logo={SaveIcon}
        />
        <SidebarActionButton>
          <SidebarActionButton.Icon>
            <SaveIcon />
          </SidebarActionButton.Icon>
          <SidebarActionButton.Text>Новое заявление</SidebarActionButton.Text>
        </SidebarActionButton>
        <AsideNav>
          <AsideNavDropdown
            text="Документы"
            Icon={SaveIcon}
          >
            <AsideNavDropdown.Item text="Входящие" to="/documents/incoming" />
            <AsideNavDropdown.Item text="Исходящие" to="/documents/outgoing" />
            <AsideNavDropdown.Item text="Черновики" to="/documents/drafts" />
          </AsideNavDropdown>
        </AsideNav>
        <CurrentUserInfo avatarAlt="AA" avatarSrc={null} userName="userName">
          <CurrentUserInfo.Item Icon={SaveIcon} text="Выйти" />
        </CurrentUserInfo>
      </Sidebar>
  </MemoryRouter>
</Playground>
