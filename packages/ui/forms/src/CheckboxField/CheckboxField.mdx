---
menu: forms
name: CheckboxField
---

import Form from '../Form';
import { LabelCheckbox } from './CheckboxField.docz';
import CheckboxField from './CheckboxField';
import { Playground, Props } from 'docz'

# CheckboxField

<Props of={CheckboxField} />

## Basic code

```
<CheckboxField label="CheckboxField" name="CheckboxField" />
```

## Without label

<Playground>
  <Form onSubmit={(values) => console.log('onSubmit', values)}>
    {() => (<CheckboxField name="check" />)}
  </Form>
</Playground>

## With label

<Playground>
  <Form onSubmit={(values) => console.log('onSubmit', values)}>
    {() => (
     <div style={{ display: 'grid', 'grid-template-columns': 'auto', 'justify-content': 'start', 'grid-gap': '20px' }}>
        <LabelCheckbox name="check1" label="end label checkbox" />
        <LabelCheckbox name="check2" label="start label checkbox" labelPlacement="start" />
        <LabelCheckbox name="check3" label="top label checkbox" labelPlacement="top" />
        <LabelCheckbox name="check4" label="top label checkbox" labelPlacement="bottom" />
      </div>
    )}
  </Form>
</Playground>

## Disabled

<Playground>
  <Form onSubmit={(values) => console.log('onSubmit', values)}>
    {() => (<LabelCheckbox disabled name="disabled" label="disabled checkbox" />)}
  </Form>
</Playground>

## Errors

<Playground>
  <Form initialValues={{ errorField: false }} onSubmit={(values) => console.log('onSubmit', values)}>
    {() => (
      <>
        <LabelCheckbox required name="errorField" label="error checkbox" />
        <button type="submit">submit</button>
      </>
    )}
  </Form>
</Playground>
