---
menu: forms
name: KPPField
---

import { Props, Playground } from 'docz';
import Form from '../Form';
import KPPField from './index';

# KPPField

<Props of={KPPField} />

## Basic code

```
<KPPField />
```

## Basic usage

<Playground>
  <a href="https://text-mask.github.io/text-mask/" target="_blank">text-mask</a>
  <Form onSubmit={(values) => console.log('onSubmit', values)}>
    {(formProps) => console.log('formProps', formProps) || (
    <>
      <KPPField variant="others" label="others" name="others" />
      <KPPField variant="organization" label="organization" name="organization" />
    </>
    )}
  </Form>
</Playground>
