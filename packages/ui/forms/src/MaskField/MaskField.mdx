---
menu: forms
name: MaskInput
---

import { Props, Playground } from 'docz';
import Form from '../Form';
import MaskInput from './MaskInput';

# MaskInput

<Props of={MaskInput} />

## Basic code

```
<MaskInput
 name="maskField" mask={[
   '+',
   /\d/,
   /\d/,
  ]}
/>
```

## Basic usage

<Playground>
  <a href="https://text-mask.github.io/text-mask/" target="_blank">text-mask</a>
  <Form onSubmit={(values) => console.log('onSubmit', values)}>
    {(formProps) => console.log('formProps', formProps) || ( <MaskInput 
                name="maskField" 
                label="label"
                placeholder="placeholder"
                mask={[
                 '+',
                 /\d/,
                 /\d/,
              ]}
              />)}
  </Form>
</Playground>
